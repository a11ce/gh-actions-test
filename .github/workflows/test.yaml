name: "Do Things"
on:
  workflow_dispatch:

jobs:
  thing:
    name: Do Something
    runs-on: macos-13
    steps:
      - name: install macports and freetype
        uses: melusina-org/setup-macports@v1
        with:
          parameters: '.github/macports/macports.yaml'
      - name: test
        run: |
          port
